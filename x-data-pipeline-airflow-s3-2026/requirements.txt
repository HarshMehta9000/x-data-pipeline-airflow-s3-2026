# Core ETL (required for both mock and live runs)
pandas==2.2.2
pyarrow==16.1.0

# Orchestration
apache-airflow==2.9.3

# Live X (Twitter) API v2 — only needed when MOCK_MODE=0
tweepy==4.14.0

# Live S3 load — only needed when MOCK_MODE=0 and S3_BUCKET is set
boto3==1.34.140

# Optional: better sentiment than the built-in fallback lexicon
vaderSentiment==3.3.2

# Tests
pytest==8.2.2
